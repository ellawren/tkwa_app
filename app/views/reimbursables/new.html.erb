  	<% provide :title, "Add New Reimbursable" %>
  	<%= link_to "Reimbursables", reimbursables_path, class: "header-label" %>
  	<div class="page-header">
    	<h1>Add New Reimbursable</h1>
  	</div>

<%= form_for @reimbursable do |f| %>

  	<%= render 'shared/error_messages', object: f.object %>
  	<%= render 'fields', f: f %>

  	<div class="save-row">
        <%= f.submit "Add Reimbursable", class: "btn btn-primary save-btn" %>
    </div>

<% end %>
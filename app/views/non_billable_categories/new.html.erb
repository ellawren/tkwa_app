    <% provide :title, "New Non-Billable Category" %>
    <%= link_to "Non-Billable Categories", non_billable_categories_path, class: "header-label" %>
    <div class="page-header">
        <h1>New Non-Billable Category</h1>
    </div>

<%= form_for @non_billable_category do |f| %>

    <%= render 'shared/error_messages', object: f.object %>
    <%= render 'fields', f: f %>

    <div class="save-row">
        <%= f.submit "Create Non-Billable Category", class: "btn btn-primary save-btn" %>
    </div>

<% end %>

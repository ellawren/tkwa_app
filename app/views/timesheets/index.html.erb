<% content_for :page_header do %>
	<% provide :title, "Timesheets" %>
	<div class="page-header">
		<h1><%= current_user.name %></h1>
	</div>
<% end %>

<%= @test %>


<div class="row icons">

	<div class="tile-box">
        <%= link_to image_tag("data.png", :alt => "timesheets", :class => "round-icon"), "/users/#{current_user.id}/timesheets/#{@curr_year}/#{@curr_week}" %>
        <%= link_to "Timesheets", "/users/#{current_user.id}/timesheets/#{@curr_year}/#{@curr_week}", :class => "tile-label" %>
    </div>

    <div class="tile-box">
        <%= link_to image_tag("forecast.png", :alt => "forecast", :class => "round-icon"), "#{}" %>
        <%= link_to "Forecast", "#{}", :class => "tile-label" %>
    </div>

    <div class="tile-box disabled">
        <%= link_to image_tag("mail.png", :alt => "mailing_lists", :class => "round-icon"), "#" %>
        <%= link_to "Expense Reports", "#", :class => "tile-label" %>
    </div>

    <div class="tile-box disabled">
        <%= link_to image_tag("target.png", :alt => "mailing_lists", :class => "round-icon"), "#" %>
        <%= link_to "Targets", "#", :class => "tile-label" %>
    </div>

</div> <!-- end tiles -->

<h2><%= Date.today.year %> Holidays</h2>

	<table class="table">
		<% Holiday.all.each do |h| %>
			<tr>
				<td><%= h.name %></td>
				<td><%= h.date.strftime("%a, %b %e, %Y") %></td>
			</tr>
		<% end %>
	</table>

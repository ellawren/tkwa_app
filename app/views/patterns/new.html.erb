    <% provide :title, "Add Pattern" %>
    <div class="page-header">
      <h1>New Pattern</h1>
    </div>

<%= form_for(@pattern, :html => { :multipart => true }) do |f| %>
        
    <%= render 'fields', f: f %>

    <div class="save-row">
        <%= f.submit "Create Pattern", class: "btn btn-primary save-btn" %>
    </div>

<% end %>

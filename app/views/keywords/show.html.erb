<h1><%= @keyword.name %><%= link_to "edit category", edit_keyword_path(@keyword), class: "btn right" %></h1>

<h2>Patterns with this keyword</h2>

<ul id="pattern-tiles" >
	<% @keyword.patterns.each do |param| %>
			<%= render 'pattern', :f => param %>
	<% end %>
</ul>	

<%= form_for @contact do |f| %>
<%= render 'shared/error_messages', object: f.object %>

   <div class="content">

        <%= render 'fields', :f => f %>

      <!--  <h2>Notes</h2>
        <div class="full contact">
                <%= f.label(:notes, "Notes", class: "input-label") %>
                <%= f.text_area :notes, class: "", rows: "8" %>
        </div>  -->

        <div class="save-row">
            <%= f.submit "Update", class: "btn btn-primary save-btn" %>
        </div> 

    </div> <!-- end content -->

<% end %>
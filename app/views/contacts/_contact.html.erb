					<tr class="results-row">
		                <td class="bold"><% if f.full_name %><%= link_to f.full_name, contact_path(f) %><% else %><%= link_to "", contact_path(f) %><% end %></td>
		                <td><%= link_to f.company_name, contact_path(f) %></td>
		            </tr>